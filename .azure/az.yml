trigger: none

schedules:
- cron: "0 8-12 * * *"
  displayName: Daily midnight build
  branches:
    include:
    - master
  always: "true"

variables:
- group: huawei

jobs:

- job: build
  timeoutInMinutes: 50

  pool:
    vmImage: 'ubuntu-latest'

  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.7'

  - script: |
      pip install azure-cli
      az extension add --name azure-devops
    displayName: 'Install azure-cli'

  - script: |
      python3 main.py --client huawei --username ${{ secrets.AZ_USERNAME }} --password ${{ secrets.AZ_PASSWORD }}
    displayName: 'Run'
